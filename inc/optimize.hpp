#pragma once

#include "ast.hpp"

// B.2.4: оптимизации на уровне AST.
// Свёртка констант, удаление мёртвых веток (if по константному условию,
// while(false)) и алгебраические упрощения (x+0, x*1, x*0 без побочных эффектов).
// Каждое преобразование сохраняет семантику программы.
void optimize_program(Program *prog);
